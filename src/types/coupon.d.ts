export type Coupon = {
    id: number
    name: string
    code: string
    type: string
    value: number
    minOrder: number
    maxDiscount: number
    description: string
    expired: string
    status: string
}